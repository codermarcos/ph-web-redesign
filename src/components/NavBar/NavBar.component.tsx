import {
  HeaderContainer,
  LoginLink,
} from '@/components/Header/Header.styles';

function NavBar() {

  return (
    <nav>
      <menu>
        <li />

        <li />
      </menu>
    </nav>
  );

}

export default NavBar;
